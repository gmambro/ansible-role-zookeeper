---
- hosts: localhost
  remote_user: root
  roles:
    - name: gmambro.zookeeper
      vars:
        zookeeper_node_id: 1
  tasks:
    - name: Get Facts
      zookeeper_facts:

    - debug:
        var: zookeeper_facts
